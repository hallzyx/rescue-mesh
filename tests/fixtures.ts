export { EXAMPLE_EN, EXAMPLE_ES, EXAMPLE_LOCATION } from "../lib/demo-examples";

export const ROUTES = [
  "/",
  "/demo",
  "/plan",
  "/reporter",
  "/reporter/report",
  "/reporter/reports",
  "/reporter/network",
  "/responder",
  "/responder/network",
] as const;
